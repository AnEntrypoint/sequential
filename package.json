{
  "name": "sequential",
  "version": "1.0.0",
  "description": "Pause/resume executor for edge functions - save to DB, exit, resume on HTTP callback",
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "files": ["src"],
  "engines": {"node": ">=18.0.0"},
  "scripts": {"dev": "node --watch example.js"},
  "dependencies": {
    "nanoid": "^5.0.0"
  }
}
